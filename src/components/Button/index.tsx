import React from 'react';

import {Stack, Button} from '@mui/material';

const AppButton = () => {
  return (
    <Stack>
      <Button>title</Button>
    </Stack>
  );
};

export default AppButton;
